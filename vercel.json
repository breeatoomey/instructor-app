{
  "version": 2,
  "builds": [
    {
      "src": "server/api/index.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "server/api/(.*)",
      "dest": "server/api/index.js"
    }
  ],
  "rewrites": [{ "source": "server/api/(.*)", "destination": "server/api" }]
}