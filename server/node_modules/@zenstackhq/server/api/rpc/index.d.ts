import { Response } from '../../types';
import { RequestContext } from '../base';
export default function makeHandler(): ({ prisma, method, path, query, requestBody, modelMeta, zodSchemas, logger, }: RequestContext) => Promise<Response>;
export { makeHandler as RPCApiHandler };
